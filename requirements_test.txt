# Superseded by requirements_test_min.txt / requirements_test_latest.txt.
#
# CI now tests both ends of the rolling ~3-month HA support window directly
# (see .github/workflows/test.yml and README.md "Supported Home Assistant
# versions"), so this file is no longer installed by CI. It's kept only as a
# convenience pointer to the "latest" lane, for existing local dev muscle
# memory (`uv pip install -r requirements.txt -r requirements_test.txt`).
# Prefer installing requirements_test_min.txt or requirements_test_latest.txt
# directly -- each is self-contained (it pins its own homeassistant,
# python-rako-2025 and colorlog, so requirements.txt is not needed alongside
# it).
-r requirements_test_latest.txt

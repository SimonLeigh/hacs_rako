# Test-only dependencies for WP-2.4 (custom_components/rako's own test suite).
# `requirements.txt` covers the runtime deps (homeassistant, python-rako-2025);
# this file adds what running `pytest` needs on top of those.
#
# Install with:
#   uv venv .venv --python 3.13
#   uv pip install --python .venv/bin/python -r requirements.txt -r requirements_test.txt
pytest-homeassistant-custom-component==0.13.316
pytest-cov==7.0.0
